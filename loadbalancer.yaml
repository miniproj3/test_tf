apiVersion: v1
kind: Service
metadata:
  name: flask-service-loadbalancer
    #annotations:
    #  service.beta.kubernetes.io/aws-load-balancer-type: ""   # nlb 또는 alb
spec:
  selector:
    app: flask
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
