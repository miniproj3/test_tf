# RDS 비밀번호는 AWS Secrets Manager에서 가져오기
apiVersion: v1
kind: Secret
metadata:
  name: rds-secret
  namespace: default
type: Opaque
stringData:
  MYSQL_PASSWORD: "{{ .Values.database.password }}"  # Helm 사용 시 예제, 실제 적용 시 대체 필요
